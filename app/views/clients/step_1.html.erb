
  <h2 class="section-title" style="margin-bottom:50px !important">Support Solutions Client Application</h2>

  <%= form_with(model: @client, url: register_step_1_path, local: true, method: :post) do |form| %>
    <%= render 'step_1', form: form, client: @client %>
    <div class="w3-container w3-card">
      <%= form.submit "Complete Step 1", class:"button btn-block margin-sm" %>
    </div>
  <% end %>
