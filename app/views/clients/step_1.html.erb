
<h1 class="fancy-title">New Client Application</h1>

 <%= form_with(model: @client, url: register_step_1_path, local: true, method: :post) do |form| %>
    <%= render 'step_1', form: form, client: @client %>
    <div class="container"><%= form.submit "Complete Step 1", class:"button hvr-underline-from-center header-photo-btn", id: "step_2_submit" %></div>
 <% end %>
