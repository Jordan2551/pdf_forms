
  <%= form_with(model: @client, url: register_step_2_path, local: true, method: :post) do |form| %>
    <%= render 'step_2', form: form, client: @client %>
    <div class="w3-container w3-card">
      <%= form.submit "Complete Step 2", class:"button btn-block margin-sm" %>
    </div>
  <% end %>
